apiVersion: v1
kind: Service
metadata:
  name: {{ include "customchart2.fullname" . }}
  labels:
    {{- include "customchart2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customchart2.selectorLabels" . | nindent 4 }}
